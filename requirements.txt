# One environment: `antsreg`.
#
# It already exists (see ../Registration_ants/requirements.txt). Every tool here
# imports `registration_ants` through its editable install there, and
# registration_eval.py needs antspyx for point transforms and Jacobians, so they
# belong in that env rather than getting one of their own:
#
#   paint_mask.py  single_sample.py  registration_eval.py
#   tools/atlas_view.py  tools/edit_sample_labels.py
#   tools/qc_guide_mask.py  tools/convert_regions_ontology.py
#
# If antsreg ever has to be rebuilt, these are what they need on top of
# ../Registration_ants/requirements.txt -- which is nothing, it already covers
# all of them:
napari[pyqt5]
SimpleITK
numpy
scipy
scikit-image
pandas
pyyaml
tifffile
